#version 330 core

attribute vec3 position;
//attribute vec4 inColor;
attribute vec2 vTexCoord0;
varying vec2 vTex;
//varying vec4 color; 

void main()
{
	gl_Position = vec4(position, 1.0);
	//color = inColor;
	vTex = vTexCoord0;
}